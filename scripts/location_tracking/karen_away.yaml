alias: Karen away
sequence:
# Everybody has a boolean that defines if they're home or away. They're home, 
#  so turn it on
- service: input_boolean.turn_off
  entity_id: input_boolean.karen_home